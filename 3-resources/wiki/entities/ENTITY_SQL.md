---
id: ENTITY_SQL
type: entity
aliases: ["SQL", "Structured Query Language"]
tags: ["data-science", "tools", "database"]
---

# 🏢 Thực thể: SQL (Structured Query Language)

## 1. Định nghĩa cốt lõi
**SQL (Structured Query Language)** là ngôn ngữ tiêu chuẩn dùng để giao tiếp, quản lý và thao tác với các hệ quản trị cơ sở dữ liệu quan hệ (RDBMS). Trong Khoa học Dữ liệu, SQL là kỹ năng bắt buộc để trích xuất (extract) và biến đổi (transform) dữ liệu từ các kho chứa dữ liệu lớn.

## 2. Vai trò trong Data Science
- **Truy vấn dữ liệu**: Trích xuất dữ liệu thô từ các cơ sở dữ liệu (MySQL, PostgreSQL, SQL Server, v.v.).
- **Tiền xử lý (Data Prep)**: Lọc, gom nhóm (GROUP BY), nối bảng (JOIN) trước khi đưa vào các công cụ phân tích hoặc mô hình học máy.
- **Khám phá dữ liệu**: Nhanh chóng thống kê và xem xét các mẫu (patterns) cơ bản.

## 3. Các thực thể liên quan (Cross-References)
-   **Ngôn ngữ/Công cụ khác**: [[ENTITY_Python]]
-   **Lĩnh vực**: [[ENTITY_Data_Science]]

---
[AUDITOR] Rule 14: Trang thực thể cơ bản được tạo dựa trên kiến thức chung ngành dữ liệu.
