---
title: "Insight: Infrastructure Hardening & Database Synchronization"
type: insight
status: VERIFIED
last_reconciled: "2026-05-14"
tags: ["infrastructure", "sqlite", "mcp", "synchronization", "hardening"]
---

# INSIGHT: Infrastructure Hardening & Database Synchronization

**Date:** 2026-05-14
**Context:** Phục hồi tính nhất quán giữa SQLite và Hệ thống file vật lý.

## 1. Phát hiện sai lệch (Infrastructure Drift)
- **Vấn đề:** Có sự chênh lệch lớn giữa số lượng Atoms trong DB và số lượng file thực tế trong `3-resources/wiki/`.
- **Nguyên nhân:** 
    - Rule **R11** và script **rebuild** bỏ qua các file không có khối Frontmatter `---`.
    - Các file `session_insights` và một số `synthesis` cũ được tạo thủ công hoặc qua các script đời đầu thiếu định dạng này.
- **Hậu quả:** Tri thức thực tế tồn tại trên đĩa nhưng "vô hình" trước các công cụ truy vấn (`wiki-query`) và không thể tham chiếu chéo.

## 2. Giải pháp triển khai
- **Chuẩn hóa Metadata (Normalization):** Dùng script Python (`fix_frontmatter.py`) quét và bổ sung Frontmatter cho toàn bộ các "Physical Orphans".
- **MCP Integration:** Tích hợp `sqlite` và `git` (local) vào Antigravity để cho phép Agent kiểm tra trạng thái DB và Repository nhanh chóng mà không cần qua scripts trung gian.
- **Dashboard Upgrade:** Cập nhật `Dataview_Queries.md` lên V2.0 để tương thích với trường `type` và `source_file` thay vì `category` cũ.

## 3. Bài học kinh nghiệm (Lesson Learned)
- **Governance First:** Việc bỏ qua Logging (R2) dù chỉ trong một phiên kỹ thuật cũng làm giảm tính minh bạch của hệ thống. Luôn phải khởi tạo Log trước khi code.
- **Surgical Consistency:** ID của Atom nên khớp với tên file vật lý để tránh gây nhiễu cho các công cụ Audit.
- **Fail-Safe Design:** Script `rebuild.py` cần có cơ chế cảnh báo khi phát hiện file `.md` hợp lệ nhưng thiếu Frontmatter, thay vì âm thầm bỏ qua.

---
*Insight generated by Antigravity — 2026-05-14*
