# ============================================================
# Thư viện Python cho Pipeline Dịch PDF — Burn_Token
# ============================================================

# Gọi AI qua OpenRouter (worker + validator)
litellm>=1.35.0

# Đọc PDF và trích xuất text
pymupdf>=1.24.0

# Tạo file PDF đầu ra (hỗ trợ Unicode/tiếng Việt)
fpdf2>=2.7.9

# Validate schema JSON đầu ra
pydantic>=2.5.0

# Đọc file .env chứa API key
python-dotenv>=1.0.0

# Giao diện terminal đẹp với màu sắc và progress bar
rich>=13.7.0

# Retry logic thông minh (exponential backoff)
tenacity>=8.2.3

# Đếm token chính xác trước khi gửi lên model
tiktoken>=0.7.0

# Đọc file config.yaml
pyyaml>=6.0.1

# Tải font tự động khi chạy lần đầu (setup_fonts.py)
requests>=2.31.0
