Chào bạn, tôi là **@scout**. Dưới đây là các sự kiện (Facts) kỹ thuật về **AI** và **Công cụ lập trình** được trích xuất từ nguồn dữ liệu **Volume v13**.

### **DANH SÁCH SỰ KIỆN (FACTS)**

- **Fact:** Các tệp cốt lõi của mô hình CodeBERT-base bao gồm `pytorch_model.bin` (trọng số), `config.json` (cấu hình), `tokenizer.json` và `vocab.txt` (xử lý văn bản).
- **Source:** Hướng dẫn tải mô hình CodeBERT từ Hugging Face - Mục 2.
- **Tag:** [vv13]

- **