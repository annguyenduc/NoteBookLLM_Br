Dưới đây là các sự kiện kỹ thuật về lập trình và xử lý dữ liệu (nền tảng cho AI và Robotics) được trích xuất từ dữ liệu cung cấp:

- **Fact:** Sử dụng hàm `re.sub` với tham số là một hàm `lambda` cho phép xử lý thay thế chuỗi linh hoạt và động dựa trên đối tượng khớp (match object).
- **Source:** [v16 - Section: Điều chỉnh lại mã sử dụng lambda]
- **Tag:** [vv16]

- **Fact:** Trong Python, tiền tố `r` trước một chuỗi (raw string) giúp các ký tự escape như `\` được hiểu là ký tự thô, không bị trình thông dịch xử lý thành các ký tự điều khiển (như `\n`).
- **Source:** [v16 - Section: Phân tích r"+1-\1\2"]
- **Tag:** [vv16]

- **Fact:** Phương thức `groups()` của thư viện `re` trong Python trả về một **Tuple** chứa tất cả các nhóm (capturing groups) đã khớp được.
- **Source:** [v16 - Section: Question 3 / ASSISTANT response on groups method]
- **Tag:** [vv16]

- **Fact:** Ký hiệu `\b` trong biểu thức chính quy đại diện cho ranh giới từ (word boundary), giúp đảm bảo mẫu khớp chính xác một từ độc lập thay vì một phần của từ khác.
- **Source:** [v16 - Section: Explanation of \b]
- **Tag:** [vv16]

- **Fact:** Cú pháp `{n,m}` trong Regex (ví dụ `\d{7,8}`) được sử dụng để chỉ định số lần lặp lại tối thiểu và tối đa của một mẫu ký tự.
- **Source:** [v16 - Section: Regex Pattern (pattern) for employee ID]
- **Tag:** [vv16]

- **Fact:** Hàm `re.findall()` trả về một danh sách (List) chứa tất cả các chuỗi con khớp với mẫu tìm kiếm trong văn bản đầu vào.
- **Source:** [v16 - Section: ASSISTANT response on find_url]
- **Tag:** [vv16]

- **Fact:** Biểu thức `[a-zA-Z]` là mẫu chuẩn để khớp với bất kỳ ký tự chữ cái nào trong bảng chữ cái tiếng Anh, bao gồm cả chữ hoa và chữ thường.
- **Source:** [v16 - Section: Biểu thức chính quy các chữ cái]
- **Tag:** [vv16]

- **Fact:** Lỗi `SyntaxError: unexpected EOF while parsing` thường phát sinh do cấu trúc mã nguồn không hoàn chỉnh, phổ biến nhất là thiếu dấu đóng ngoặc `)`, `]` hoặc dấu nháy kết thúc chuỗi.
- **Source:** [v16 - Section: Common Issues that Lead to SyntaxError]
- **Tag:** [vv16]

- **Fact:** Mẫu `\S+` trong biểu thức chính quy được dùng để khớp với một hoặc nhiều ký tự liên tiếp không phải là khoảng trắng.
- **Source:** [v16 - Section: Regex Pattern (pattern) for URLs]
- **Tag:** [vv16]

- **Fact:** Để khớp với dấu chấm `.` theo nghĩa đen trong Regex, cần sử dụng dấu escape `\.` vì dấu chấm đơn lẻ là một ký tự đặc biệt đại diện cho bất kỳ ký tự nào.
- **Source:** [v16 - Section: Regex Pattern (pattern) for URLs]
- **Tag:** [vv16]