# 📊 Research Analysis: Python for Data Analysis (Wes McKinney)

## 📋 Thông tin chung
- **Tác giả:** Wes McKinney (Người tạo ra pandas).
- **Trọng tâm:** Sử dụng Python để xử lý, làm sạch và phân tích dữ liệu cấu trúc. Tập trung sâu vào NumPy và pandas.
- **Phạm vi Ingest:** Toàn bộ hệ sinh thái Scientific Python (NumPy, pandas, matplotlib).

## 🧩 Các thực thể & Khái niệm then chốt (Atoms Candidate)

### 1. Hệ sinh thái Scientific Python (Ecosystem)
- **Entity:** Các thư viện trụ cột: NumPy (Array), pandas (Tabular data), matplotlib (Plotting), IPython (Interactive), SciPy (Algorithms).

### 2. NumPy & Vectorized Computation
- **Concept:** `ndarray` và lý do nó nhanh hơn list Python (Memory layout, Vectorization).
- **Kỹ thuật:** Broadcasting (Lan truyền), Indexing/Slicing nâng cao.

### 3. pandas: Cấu trúc dữ liệu phân tích
- **Concept:** `Series` và `DataFrame`. Cơ chế căn chỉnh dữ liệu (Data Alignment) tự động dựa trên Label.
- **Wrangling:** Join, Merge, Reshape (Melt/Pivot).

### 4. GroupBy Mechanics (Split-Apply-Combine)
- **Concept:** Quy trình 3 bước: Chia dữ liệu -> Áp dụng hàm -> Kết hợp kết quả. Đây là cốt lõi của phân tích dữ liệu nhóm.

### 5. Time Series Analysis
- **Concept:** Xử lý dữ liệu thời gian (Datetime, Period). Kỹ thuật Resampling (Downsampling/Upsampling) và Window Functions (Rolling).

## 📈 Đóng góp cho Master Synthesis (Python Mastery)
- Tạo mới Master Page: `SYNTHESIS_PYTHON_Data_Mastery.md`.
- Kết nối logic: SQL (Trích xuất) -> Python (Xử lý & Wrangling) -> Excel (Mô hình hóa/Báo cáo nhanh).

## 🛠️ Mining Stats
- **Total Lines Scanned:** ~800 lines.
- **Key Concepts Identified:** 5.
- **Entities Identified:** 1 (Book Source).
